import XCTest
@testable import SwiftBinaryPackage

final class SwiftBinaryPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftBinaryPackage().text, "Hello, World!")
    }
}
